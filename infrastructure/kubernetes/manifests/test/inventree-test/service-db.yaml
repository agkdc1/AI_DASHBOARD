apiVersion: v1
kind: Service
metadata:
  name: inventree-test-db
  namespace: shinbee
spec:
  selector:
    app.kubernetes.io/name: inventree-test-db
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  clusterIP: None
