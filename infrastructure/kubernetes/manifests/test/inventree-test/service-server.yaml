apiVersion: v1
kind: Service
metadata:
  name: inventree-test-server
  namespace: shinbee
spec:
  selector:
    app.kubernetes.io/name: inventree-test-server
  ports:
    - port: 8000
      targetPort: 8000
      name: http
