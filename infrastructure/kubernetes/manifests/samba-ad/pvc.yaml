apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: samba-ad-data
  namespace: shinbee
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-single
  resources:
    requests:
      storage: 10Gi
