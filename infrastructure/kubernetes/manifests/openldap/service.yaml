apiVersion: v1
kind: Service
metadata:
  name: openldap
  namespace: shinbee
  labels:
    app.kubernetes.io/name: openldap
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: openldap
  ports:
    - name: ldap
      port: 389
      targetPort: 389
      nodePort: 30389
